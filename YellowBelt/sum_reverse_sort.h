#pragma once

#ifndef SUM_REVERSE_SORT
#define SUM_REVERSE_SORT

// Task_013

#include <string>
#include <vector>

int Sum(int x, int y);

std::string Reverse(std::string s);

void Sort(std::vector<int>& nums);

#endif // !SUM_REVERSE_SORT
